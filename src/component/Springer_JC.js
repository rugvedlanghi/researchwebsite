import React from 'react'

const Springer_JC=({title,publisher,abstract}) => {
  return (
    <div>
        <p>{title}</p>
        <p>{publisher}</p>
        <p>{abstract}</p>
    </div>
  )
}

export default Springer_JC;